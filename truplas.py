#Tuplas / tuple
t = (2 , "text", True)
print(type(t))

#acceder elemento de la tupla / access tuple element
print(t[1])

#¿se puede modificar el elemento de una tupla? --> No, esta es la diferente con una lista
# Can the element of a tuple be modified? -> No, this is the different one with a list